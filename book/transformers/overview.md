# Overview

In this chapter, you will learn the components that build up a modern Transformer architecture: tokenizer, embedding layer, attention, RMS normalization, feed-forward layer, and positional embedding.

After the component-level sections, we connect the pieces to real Hugging Face `transformers` models using Llama 3 and Gemma 4 as examples.
