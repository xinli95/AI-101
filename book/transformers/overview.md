# Overview

In this chaper, you will learn every components that builds up to the Transfomers achitecture including: tokenizer, embedding layer, RMS Normalization, Attention, Feed forward layer, positional embedding.
